# deps — verify.js is a dev-only check, the site itself has no dependencies
node_modules/

# deploy
.vercel/

# verification output
_shot_*.png
_det.json

# reference material, kept locally, not worth carrying in the repo
# (gitignore does not parse quotes; spaces are literal)
My site flow.pdf
UI & Skills.docx

# videos/ IS committed on purpose: those raw Flow clips are irreplaceable source
# material, and build-assets.py needs them to rebuild scrub.mp4 from scratch.

# build scratch
_joined_*.mp4
_concat_*.txt
_cut_*.mp4
_build.log
_c_*.png
_probe.js
_track.js

# OS
Thumbs.db
desktop.ini
.DS_Store

.vercel
.env*
